services:
  db:
    image: postgres:17
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=${DB_NAME}
    expose:
      - 5432
    ports:
      - "5432:5432"

volumes:
  db_data: {}
